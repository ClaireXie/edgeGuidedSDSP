% the total script
clc;clear;

addpath('mainCode/');

names=cell(4);
names{1}='cones';
names{2}='teddy';
names{3}='tsukuba';
names{4}='venus';

w1=8;
localSize=1;

% run the code for each image
for i=1:4
    inputFile=names{i};
    fprintf(['runnning image ',names{i},'\n']);
    mrfLearning(names{i}, w1, localSize);
end

%display quantative results
runQuantative;