% the total script
clc;clear;

addpath('mainCode/');

names=cell(4);
names{1}='cones';
names{2}='teddy';
names{3}='tsukuba';
names{4}='venus';
names{5}='11_250';
names{6}='scan042_cave_statue_smNN';

w1=8;
localSize=3;  %1
%w1=10;
%localSize=1;
scale=4;
threshold=[0.08, 0.1, 0.1, 0.1 0.05, 0.05];

% run the code for each image
for i=1:6
    inputFile=names{i};
    fprintf(['runnning image ',names{i},'\n']);
    highres{i}=mrfLearning(names, i, w1, localSize, scale, threshold(i));
end

%display quantative results
runQuantative;