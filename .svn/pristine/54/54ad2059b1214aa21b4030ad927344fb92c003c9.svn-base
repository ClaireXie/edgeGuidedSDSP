% visually compare with visual results of different methods

clc;close all;clear;

names=cell(4);
names{1}='cones';
names{2}='teddy';
names{3}='tsukuba';
names{4}='venus';

scaleFact=[4,4,16,8];
scale=4;

for i=1:4
    %figure;
    load(['middlebury/', names{i},'/s4/',names{i},'_our.mat']);
    cleanImg=double(imread(['inputs/', names{i}, '_clean.png']));
    output=depthToDisp(output_our, cleanImg, scaleFact(i));
    imwrite(uint8(output),['outputs/compare/',names{i},'_ICME.png']);
    %subplot(1,2,1);imshow(uint8(output));
    %urrent=imread(['outputs/v2/', names{i}, '2_', num2str(scale), '.png']);
    %subplot(1,2,2);imshow(uint8(current));
end